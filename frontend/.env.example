# This is an example environment file.
# Copy this to .env in the same directory to use these values.

# The WebSocket URL for the backend.
# For local development, this is not needed as it defaults to ws://localhost:8502/ws
# For production, replace with your server's public IP or domain name.
VITE_WS_URL=ws://your_remote_host_ip:8502/ws
